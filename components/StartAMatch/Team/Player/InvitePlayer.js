import React, { Component } from 'react';
import { StyleSheet, View, Text, TouchableOpacity, Button } from 'react-native';

export default class InvitePlayer extends Component {
    render() {
        return (
          <View>
            <Text>
                Hello Invite Player
            </Text>
         </View>
        );
      }
}